3/2
3 ^ 2
2 ^ (-3)
100 ** (1/2)
sqrt(100)
format((10^3) * (10^2), scientific = FALSE)

#Mathematical constants 
pi
exp(1) #exp() must have at least one arguments

#logarithms: The power to which a fixed number (the base) must be 
#raised to produce a given number.
# log(x, base = b)
log(exp(1))
log(0)
log(1000, base=10)
log(8, base=2)
log(16, base=4)

#Trigonometry
sin(pi/2)
cos(0)
